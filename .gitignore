# Секреты — никогда не коммитим
.env

# Питон
__pycache__/
*.pyc
.venv/
venv/

# Собранные данные парсеров
# JSON с юрфирмами коммитим — это публичные данные с 2ГИС (название/адрес/
# рейтинг), сайт читает их напрямую из data/ при сборке (web/lib/organizations.ts)
data/*.csv

# Системные файлы macOS
.DS_Store
